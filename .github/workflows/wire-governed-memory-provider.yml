name: governed memory provider validation

on:
  push:
    branches: [ feat/memory-governed-provider-identity ]

permissions:
  contents: write

jobs:
  wire:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/memory-governed-provider-identity
          fetch-depth: 0
      - uses: dtolnay/rust-toolchain@1.89.0
        with:
          components: rustfmt, clippy
      - name: Wire module
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path
          p = Path('crates/ccos-enterprise-memory/src/lib.rs')
          text = p.read_text()
          marker = 'mod lineage_graph;\n'
          addition = 'mod governed_provider;\npub use governed_provider::{GovernedMemoryObservation, GovernedMemoryWrite, GovernedSemanticMemoryProvider};\n\n' + marker
          assert marker in text
          assert 'mod governed_provider;' not in text
          p.write_text(text.replace(marker, addition, 1))
          PY
          cargo fmt --all
      - name: Validate
        run: |
          cargo check -p ccos-enterprise-memory
          cargo clippy -p ccos-enterprise-memory -- -D warnings
          cargo test -p ccos-enterprise-memory
      - name: Commit
        shell: bash
        run: |
          git config user.name MEMOPERF
          git config user.email memoperf@users.noreply.github.com
          git add crates/ccos-enterprise-memory/src/lib.rs crates/ccos-enterprise-memory/src/governed_provider.rs
          git commit -m 'feat(memory): expose governed provider identity'
          git fetch origin feat/memory-governed-provider-identity
          git rebase origin/feat/memory-governed-provider-identity
          git push origin HEAD:feat/memory-governed-provider-identity
