name: governed recall gate validation

on:
  push:
    branches: [ feat/memory-governed-recall-gate ]

permissions:
  contents: write

jobs:
  wire:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/memory-governed-recall-gate
          fetch-depth: 0
      - uses: dtolnay/rust-toolchain@1.89.0
        with:
          components: rustfmt, clippy
      - name: Wire module
        run: |
          python3 - <<'PY'
          from pathlib import Path
          p = Path('crates/ccos-enterprise-memory/src/lib.rs')
          text = p.read_text()
          marker = 'mod governed_provider;\n'
          addition = 'mod governed_recall;\npub use governed_recall::{admit_governed_recall, GovernedRecallGate, GovernedRecallGateError, GovernedRecallTrustPolicy};\n\n' + marker
          assert marker in text
          assert 'mod governed_recall;' not in text
          p.write_text(text.replace(marker, addition, 1))
          PY
          cargo fmt --all
      - name: Validate
        run: |
          cargo check -p ccos-enterprise-memory
          cargo clippy -p ccos-enterprise-memory -- -D warnings
          cargo test -p ccos-enterprise-memory
      - name: Commit
        run: |
          git config user.name MEMOPERF
          git config user.email memoperf@users.noreply.github.com
          git add crates/ccos-enterprise-memory/src/lib.rs crates/ccos-enterprise-memory/src/governed_recall.rs
          git commit -m 'feat(memory): expose governed recall admission gate'
          git fetch origin feat/memory-governed-recall-gate
          git rebase origin/feat/memory-governed-recall-gate
          git push origin HEAD:feat/memory-governed-recall-gate
