name: memory promotion gate validation

on:
  push:
    branches: [ feat/memory-promotion-gate ]

permissions:
  contents: write

jobs:
  wire:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/memory-promotion-gate
          fetch-depth: 0
      - uses: dtolnay/rust-toolchain@1.89.0
        with:
          components: rustfmt, clippy
      - name: Wire module
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path
          p = Path('crates/ccos-enterprise-memory/src/lib.rs')
          text = p.read_text()
          marker = 'mod recall_budget;\n'
          addition = 'mod promotion;\npub use promotion::{evaluate_memory_promotion, MemoryPromotionCandidate, MemoryPromotionError};\n\n' + marker
          assert marker in text
          assert 'mod promotion;' not in text
          p.write_text(text.replace(marker, addition, 1))
          PY
          cargo fmt --all
      - name: Validate
        run: |
          cargo check -p ccos-enterprise-memory
          cargo clippy -p ccos-enterprise-memory -- -D warnings
          cargo test -p ccos-enterprise-memory
      - name: Commit
        shell: bash
        run: |
          git config user.name MEMOPERF
          git config user.email memoperf@users.noreply.github.com
          git add crates/ccos-enterprise-memory/src/lib.rs crates/ccos-enterprise-memory/src/promotion.rs
          git commit -m 'feat(memory): gate promotion candidates on evidence'
          git fetch origin feat/memory-promotion-gate
          git rebase origin/feat/memory-promotion-gate
          git push origin HEAD:feat/memory-promotion-gate
