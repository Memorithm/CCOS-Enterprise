[package]
name = "ccos-enterprise-retention"
version = "0.1.0-pre"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false
description = "Tenant-scoped cognitive retention policy: deterministic expiration, invalidation over destructive rewriting, audit events, crash-safe continuation."

[dependencies]
ccos-enterprise-runtime = { path = "../ccos-enterprise-runtime" }
ccos-enterprise-tenancy = { path = "../ccos-enterprise-tenancy" }
serde.workspace = true
serde_json.workspace = true
sha2.workspace = true
