//! Backward-compatible execution module path.
//!
//! The implementation lives in `ccos-enterprise-execution`; this module keeps
//! existing `ccos_enterprise_sessions::execution::*` imports source-compatible.

pub use ccos_enterprise_execution::*;
