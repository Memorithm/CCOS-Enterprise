[package]
name = "ccos-enterprise-store"
version = "0.1.0-pre"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false
description = "Durable, fail-closed persistence for an Enterprise deployment: an atomically-written state snapshot plus an append-only, replayable audit journal."

[dependencies]
ccos-enterprise-runtime = { path = "../ccos-enterprise-runtime" }
ccos-core.workspace = true
serde.workspace = true
serde_json.workspace = true

[dev-dependencies]
ccos-enterprise-auth = { path = "../ccos-enterprise-auth" }
