# Cognitive Audit

Organizational audit over Core's technical audit:

- per-tenant audit queries (who asserted, who resolved, who invalidated);
- contradiction-resolution trails with policy identity;
- approval-gate trails (RequireApproval decisions and their records);
- export: tenant-scoped, digest-sealed, schema-versioned — verifiable offline
  against Core's audit log format.
